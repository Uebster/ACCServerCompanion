Primeiro de tudo, ter ciência que todos os diretórios, exceto os que tem opção de escolha onde salvar (acho que apenas o entrylist), o restante todo deve ser salvo apenas dentro da pasta do servidor, se o app não estiver com a pasta válida não deixar salvar nada, os presets sim, tudo bem salvar no diretório do app, mas atualmente está gerando um server_dir e cfg na pasta do app e duplicando várias configurações, o que é muito estranho. precisamos validar os diretórios dos arquivos de cada aba, se eles estão indo apenas para o local correto e etc, se eles estão válidos principalmente. Adicionar botão de restaurar na aba eventos. colocar tooltip sobre o restaurar e sobre o salvar, informando oq vai acontecer no botão, salvar vai salvar o arquivo tal na pasta tal, e restaurar vai carregar os dados do ultimo arquivo tal salvo.
Ou seja, precisa parar de criar uma pasta no lugar errado pra salvar arquivos json, tá virando bagunça.

trackMedalsRequirement Defines the amount of track medals that a user has to have for the given track (values 0, 1, 2, 3) safetyRatingRequirement Defines the Safety Rating (SA) that a user must have to join this server (values -1, 0, …. 99) racecraftRatingRequirement Defines the Safety Rating (RC) that a user must have to join this server (values -1, 0, …. 99)
consertar estes valores para rc e sa para -1 e feedback no tooltip, parece q -1 é pra desabilitar